const CACHE_NAME='magic-english-v4';
self.addEventListener('install',e=>{e.waitUntil(caches.open(CACHE_NAME).then(c=>{const b=self.registration.scope;const l=['index.html','manifest.json','icons/icon-192.png','icons/icon-512.png'].map(p=>new URL(p,b).href);const x=['https://fonts.googleapis.com/css2?family=Fredoka:wght@400;500;600;700&family=Nunito:wght@400;600;700;800&display=swap','https://unpkg.com/react@18/umd/react.production.min.js','https://unpkg.com/react-dom@18/umd/react-dom.production.min.js'];return Promise.allSettled([...l,...x].map(u=>c.add(u).catch(()=>{})));}));self.skipWaiting();});
self.addEventListener('activate',e=>{e.waitUntil(caches.keys().then(ks=>Promise.all(ks.filter(k=>k!==CACHE_NAME).map(k=>caches.delete(k)))));self.clients.claim();});
self.addEventListener('fetch',e=>{const u=new URL(e.request.url);if(e.request.method!=='GET')return;if(u.hostname.includes('youtube.com')||u.hostname.includes('youtu.be'))return;e.respondWith(caches.match(e.request).then(c=>{if(c)return c;return fetch(e.request).then(r=>{if(r.ok&&(u.origin===self.location.origin||u.hostname.includes('googleapis.com')||u.hostname.includes('gstatic.com')||u.hostname.includes('unpkg.com'))){const cl=r.clone();caches.open(CACHE_NAME).then(ca=>ca.put(e.request,cl));}return r;}).catch(()=>{if(e.request.destination==='document')return caches.match(new URL('index.html',self.registration.scope).href);});}));});